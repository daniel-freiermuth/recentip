Turmoil's API is counter-intuitive. In doubt, consult the docs. The functions .client() and .host() are not refering to roles in the network simulation, but to simulation roles. .client() is for spawning finite tasks while .host() is intended to be used for infinite tasks.